# AAIRA frontend

Vite + React + TypeScript + Tailwind. Data comes from `src/api/mock/` only — do not point this app at the Fastify API until wiring is requested.

```bash
npm install
npm run dev
```

Demo: `nandini@aaira.in` (owner) or `rahul@aaira.in` (Lonavala staff), password `aaira123`.
